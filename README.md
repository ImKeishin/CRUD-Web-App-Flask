# CRUD-Web-App
Web app created in Python with Flask that supports CRUD operations (Create, Read, Update, Delete) to a movie database. 

You first need to have Python, Flask and MySQL Workbench installed
Start cmd and use the following commands:

cd path\to\folder\
python -m app.main

Then hold Ctrl and click on the http link provided.

